language: python
python:
  - "2.7"
  - "3.5"
# command to install dependencies
install:
    - pip install
    - pip install pep8
# command to run tests
script:
  - pep8 --exclude=migrations --ignore=E501 mopidy_json_client